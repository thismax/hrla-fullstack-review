import React, { Component } from 'react';
import PropTypes from 'prop-types';

// TODO create statefull AddQuestion component
  // state: disable = boolean
  //        answer1 = string
  //        answer2 = string
  //        answer3 = string

  // TODO create a method to disable buttons after clicked and change color if right or wrong
    // use provide css classes: right = change button to green
    //                          wrong = change button to red


  /* TODO jsx to render
    <div className="questionEntry">
      <img className="filmFrame" src={} alt="Movie frame not found" />
      <button
        className={`answerButton ${}`}
        disabled={}
        onClick={}
      >{}</button>
      <button
        className={`answerButton ${}`}
        disabled={}
        onClick={}
      >{}</button>
      <button
        className={`answerButton ${}`}
        disabled={}
        onClick={}
      >{}</button>
    </div>
  */
